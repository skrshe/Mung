package game

import "core:math"
btheta: int

settheta :: proc(theta: u32) {
    //using math
    //btheta=theta
    //if theta==2 {
    //    bdx=0.5*sign32(bdx)
    //    bdy=1.3*sign32(bdy)
    //} else if theta==0 {
    //    bdx=1.3*sign32(bdx)
    //    bdy=0.5*sign32(bdy)
    //} else {
    //    bdx=1*sign32(bdx)
    //    bdy=1*sign32(bdy)
    //}
}

